# COMPSCI-8-Formative-Assesment-3
Made by Geonnelle C. Dimalig, 8-Mahogany
*
`Problem 1:

`length ← LENGTH(password)
`IF length >= 8 AND length <= 15 THEN
`PRINT "Password length is valid"
`ELSE
`PRINT "Password too short or too long. Please try again."
`ENDIF
`END
*
`Problem 2:

`START
`INPUT credit_score
`INPUT income
`INPUT years_at_job
`IF credit_score >= 700 THEN
`IF income >= 30000 THEN
`IF years_at_job >= 2 THEN
`PRINT "Loan Approved"
`ELSE
`PRINT "Loan Denied"
`ENDIF
`ELSE
`PRINT "Loan Denied"
`ENDIF
`ELSE
`PRINT "Loan Denied"
`ENDIF
`END
